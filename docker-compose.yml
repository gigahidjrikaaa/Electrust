version: '3'
services:
  frontend:
    build:
      context: ./frontend/electrust
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    depends_on:
      - backend

  backend:
    build:
      context: ./backend/electrust-api
      dockerfile: Dockerfile
    ports:
      - 8080:8080